# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual envs
.venv/
.venv-lake/
.venv-nmnist/
.venv-arm/
venv/
env/

# Local working docs (not repo artifacts)
*.docx

# Data + model artifacts (not committed — fetched/trained locally)
data/
*.pt
*.pth
*.npz
MNIST/

# Terraform (local state + provider cache — never commit state, it can hold sensitive detail)
infra/.terraform/
infra/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars

# Tooling output
graphify-out/
.graphify*/

# OS
.DS_Store
Thumbs.db
